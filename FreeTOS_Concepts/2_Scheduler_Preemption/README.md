# Scheduler Preemption Example

This project shows how FreeRTOS handles **preemptive scheduling**. A higher-priority task will interrupt a lower-priority one, demonstrating how task priorities affect CPU time.
